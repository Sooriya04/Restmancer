<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>🧙‍♂️ Restmancer API – Blogs</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #0f172a;
        color: #f8fafc;
        padding: 2rem;
      }
      h1, h2 {
        color: #38bdf8;
      }
      code {
        background: #1e293b;
        padding: 2px 5px;
        border-radius: 4px;
        color: #facc15;
      }
      a {
        color: #60a5fa;
      }
      .tabs {
        background: #1e293b;
        border-radius: 0.5rem;
        padding: 1rem;
      }
      .nav-tabs {
        border: none;
        margin-bottom: 1rem;
      }
      .nav-tabs .nav-link {
        color: #cbd5e1;
        border: none;
        border-radius: 0;
        background-color: transparent;
      }
      .nav-tabs .nav-link.active {
        background-color: #0f172a;
        color: #38bdf8;
        border-bottom: 2px solid #38bdf8;
      }
      pre {
        background-color: #0f172a;
        border-radius: 0.5rem;
        padding: 1rem;
        color: #f8fafc;
        font-size: 0.9rem;
        overflow-x: auto;
        position: relative;
        margin-bottom: 0;
      }
      .copy-btn {
        position: absolute;
        top: 0.75rem;
        right: 1rem;
        background: #334155;
        color: #f8fafc;
        border: none;
        padding: 0.3rem 0.6rem;
        font-size: 0.75rem;
        border-radius: 0.3rem;
        cursor: pointer;
        transition: background 0.2s ease-in-out;
      }
      .copy-btn:hover {
        background: #475569;
      }
      .info-box {
        background: #1e293b;
        padding: 1rem;
        border-radius: 0.5rem;
        margin-bottom: 2rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-4">🧙‍♂️ Restmancer API – Blog Endpoints</h1>
        <p>
        <a href="/" class="btn btn-outline-info btn-sm mb-4">🔙 Back to Homepage</a>
        </p>
      <p>This page documents the available REST endpoints for fetching blog post data from the Restmancer API.</p>

      <hr />

      <h2>GET <code>/api/blogs</code></h2>
      <p>Returns a list of blog posts. If the <code>limit</code> is greater than initial data length (10), more will be generated on the fly.</p>
      <h5>Optional Query Parameters:</h5>
      <ul>
        <li><code>limit</code> – Number of blogs to return.</li>
        <li><code>field</code> – Comma-separated list of fields (e.g., <code>title,content</code>).</li>
      </ul>
      <h6>Examples:</h6>
      <ul>
        <li><a href="/api/blogs" target="_blank">/api/blogs</a></li>
        <li><a href="/api/blogs?limit=5" target="_blank">/api/blogs?limit=5</a></li>
        <li><a href="/api/blogs?field=title,slug&limit=3" target="_blank">/api/blogs?field=title,slug&limit=3</a></li>
      </ul>

      <hr />

      <h2>📂 Example Fields Available</h2>
      <ul>
        <li><code>id</code></li>
        <li><code>title</code></li>
        <li><code>slug</code></li>
        <li><code>author</code> (object with <code>id</code>, <code>name</code>, <code>email</code>)</li>
        <li><code>content</code></li>
        <li><code>tags</code></li>
        <li><code>createAt</code></li>
        <li><code>likes</code></li>
        <li><code>comments</code> (array of objects with <code>id</code>, <code>author</code>, <code>content</code>, <code>postedAt</code>)</li>
      </ul>

      <div class="info-box">
        <p>
          You can control the number of blogs returned and which fields are included using
          query parameters like <code>?limit=3</code> and <code>?field=title,slug</code>.
        </p>
        <p>Example: <code>GET /api/blogs?field=title,slug&limit=3</code></p>
      </div>

      <div class="tabs">
        <ul class="nav nav-tabs" id="langTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#python" type="button" role="tab">Python</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#js" type="button" role="tab">JavaScript</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#node" type="button" role="tab">Node.js</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#curl" type="button" role="tab">cURL</button>
          </li>
        </ul>

        <div class="tab-content mt-3" id="langTabsContent">
          <div class="tab-pane fade show active" id="python" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>import requests

url = "http://localhost:8070/api/blogs?limit=3"
response = requests.get(url)
print(response.json())</code></pre>
          </div>

          <div class="tab-pane fade" id="js" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>fetch("http://localhost:8070/api/blogs?field=title,slug&limit=2")
  .then(res => res.json())
  .then(data => console.log(data));</code></pre>
          </div>

          <div class="tab-pane fade" id="node" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>const axios = require("axios");

axios.get("http://localhost:8070/api/blogs")
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });</code></pre>
          </div>

          <div class="tab-pane fade" id="curl" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>curl "http://localhost:8070/api/blogs?field=title,slug&limit=3"</code></pre>
          </div>
        </div>
      </div>

      <hr />
      <footer class="mt-4">
        <p class="">© 2025 Restmancer API</p>
      </footer>
    </div>

    <script>
      function copyCode(btn) {
        const code = btn.nextElementSibling.innerText;
        navigator.clipboard.writeText(code).then(() => {
          btn.innerText = "Copied!";
          setTimeout(() => (btn.innerText = "Copy"), 1500);
        });
      }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
