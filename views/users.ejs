<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>🧙‍♂️ Restmancer API – Users</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #0f172a;
        color: #f8fafc;
        padding: 2rem;
      }
      h1, h2 {
        color: #38bdf8;
      }
      code {
        background: #1e293b;
        padding: 2px 5px;
        border-radius: 4px;
        color: #facc15;
      }
      a {
        color: #60a5fa;
      }
      h1 {
        color: #38bdf8;
        margin-bottom: 1rem;
      }

      .tabs {
        background: #1e293b;
        border-radius: 0.5rem;
        padding: 1rem;
      }

      .nav-tabs {
        border: none;
        margin-bottom: 1rem;
      }

      .nav-tabs .nav-link {
        color: #cbd5e1;
        border: none;
        border-radius: 0;
        background-color: transparent;
      }

      .nav-tabs .nav-link.active {
        background-color: #0f172a;
        color: #38bdf8;
        border-bottom: 2px solid #38bdf8;
      }

      pre {
        background-color: #0f172a;
        border-radius: 0.5rem;
        padding: 1rem;
        color: #f8fafc;
        font-size: 0.9rem;
        overflow-x: auto;
        position: relative;
        margin-bottom: 0;
      }

      .copy-btn {
        position: absolute;
        top: 0.75rem;
        right: 1rem;
        background: #334155;
        color: #f8fafc;
        border: none;
        padding: 0.3rem 0.6rem;
        font-size: 0.75rem;
        border-radius: 0.3rem;
        cursor: pointer;
        transition: background 0.2s ease-in-out;
      }

      .copy-btn:hover {
        background: #475569;
      }

      .info-box {
        background: #1e293b;
        padding: 1rem;
        border-radius: 0.5rem;
        margin-bottom: 2rem;
      }

      code {
        background: #334155;
        padding: 0.2rem 0.4rem;
        border-radius: 0.3rem;
        font-size: 0.95rem;
        color: #facc15;
      }

    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-4">🧙‍♂️ Restmancer API – User Endpoints</h1>

      <p>This page documents the available REST endpoints for fetching user data from the Restmancer API.</p>

      <hr />

      <h2>GET <code>/api/users</code></h2>
      <p>Returns a list of all users.</p>
      <h5>Optional Query Parameters:</h5>
      <ul>
        <li><code>limit</code> – Number of users to return.</li>
      </ul>
      <h6>Examples:</h6>
      <ul>
        <li><a href="/api/users" target="_blank">/api/users</a></li>
        <li><a href="/api/users?limit=3" target="_blank">/api/users?limit=3</a></li>
      </ul>

      <hr />

      <h2>GET <code>/api/users/custom</code></h2>
      <p>Returns only specific fields of users, optionally with a limit.</p>
      <h5>Required Query Parameters:</h5>
      <ul>
        <li><code>field</code> – Comma-separated list of fields (e.g., <code>name,email</code>).</li>
      </ul>
      <h5>Optional:</h5>
      <ul>
        <li><code>limit</code> – Limit number of users to return.</li>
      </ul>
      <h6>Examples:</h6>
      <ul>
        <li><a href="/api/users/custom?field=name,email" target="_blank">/api/users/custom?field=name,email</a></li>
        <li><a href="/api/users/custom?field=name,email&limit=2" target="_blank">/api/users/custom?field=name,email&limit=2</a></li>
      </ul>

      <hr />

      <h2>📂 Example Fields Available</h2>
      <ul>
        <li><code>id</code></li>
        <li><code>name</code></li>
        <li><code>username</code></li>
        <li><code>email</code></li>
        <li><code>password</code></li>
        <li><code>role</code></li>
        <li><code>location</code></li>
        <li><code>skills</code></li>
      </ul>
          <div class="container">
      <h1>📘 Restmancer – Users API</h1>

      <div class="info-box">
        <p>
          The <code>/api/users</code> endpoint supports custom field filtering
          and limiting results. Use the <code>?field=name,email</code> and
          <code>&limit=2</code> query parameters to customize your output.
        </p>
        <p>Example: <code>GET /api/users?field=name,email&limit=2</code></p>
      </div>

      <div class="tabs">
        <ul class="nav nav-tabs" id="langTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#python" type="button" role="tab">Python</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#js" type="button" role="tab">JavaScript</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#node" type="button" role="tab">Node.js</button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#curl" type="button" role="tab">cURL</button>
          </li>
        </ul>

        <div class="tab-content mt-3" id="langTabsContent">
          <div class="tab-pane fade show active" id="python" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>import requests

url = "http://localhost:8070/api/users"
response = requests.get(url)
print(response.json())</code></pre>
          </div>

          <div class="tab-pane fade" id="js" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>fetch("http://localhost:8070/api/users?field=name,email&limit=2")
  .then(res => res.json())
  .then(data => console.log(data));</code></pre>
          </div>

          <div class="tab-pane fade" id="node" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>const axios = require("axios");

axios.get("http://localhost:8070/api/users")
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });</code></pre>
          </div>

          <div class="tab-pane fade" id="curl" role="tabpanel">
            <pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>curl "http://localhost:8070/api/users"</code></pre>
          </div>
        </div>
      </div>
    </div>
      <hr />
      <footer class="mt-4">
        <p class="">© 2025 Restmancer API</p>
      </footer>
    </div>
  </body>
    <script>
      function copyCode(btn) {
        const code = btn.nextElementSibling.innerText;
        navigator.clipboard.writeText(code).then(() => {
          btn.innerText = "Copied!";
          setTimeout(() => (btn.innerText = "Copy"), 1500);
        });
      }
    </script>
  
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</html>
